enum AdminAccess { yes, no }
enum Environment { dev, stage, prod }
enum socialLogin { google, facebook, apple, normal }
enum verifyEmailResend { verifyEmail, resendOtp }
enum fromSignInType { signIn, signUp }
enum listType { list, button }
enum profileDetailType { mysaved, match, notification, projectDetail, invited }
enum CancelRideReasons {noCallResponse, passengerAway, notPresent, wrongAddress, riderNotReady, lowRiderScore}